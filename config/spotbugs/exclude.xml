<?xml version="1.0" encoding="UTF-8"?>
<!--
  SpotBugs exclude filter. Kept intentionally minimal — every entry below is a specific,
  investigated finding with a documented reason it isn't a real defect here, not a blanket
  category suppression. Never add an entry to silence a real finding without investigating it
  first.
-->
<FindBugsFilter>
    <!--
      EI_EXPOSE_REP2 on TenantAdminNotifier(UserRepository, NotificationService): flags storing
      the injected UserRepository reference into a field. This is plain constructor injection of
      a stateless platform repository interface — the same pattern GuardianService uses for its
      EventPublisher field (also a platform interface) without SpotBugs flagging it. Investigated
      2026-08-16: no other structurally identical constructor in this module is flagged, so this
      is detector noise on a standard, safe DI pattern, not a real defect — there is no mutable
      state to leak, and the reference is never handed back to a caller.
    -->
    <Match>
        <Class name="com.altafjava.school.application.scheduler.support.TenantAdminNotifier"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on ReportCardService's 10-arg constructor: flags storing the injected
      StorageService (a platform interface) and ReportCardPdfGenerator (a stateless, school-local
      formatting component) references into fields. Same standard constructor-injection pattern
      as every other service in this module — investigated 2026-08-16, alongside the
      TenantAdminNotifier finding above: this detector flags EI_EXPOSE_REP2 inconsistently across
      structurally identical constructors (interface-typed and concrete-typed alike) elsewhere in
      this exact module, which is detector noise, not a real defect — neither field holds mutable
      state that could leak, and neither is ever handed back to a caller.
    -->
    <Match>
        <Class name="com.altafjava.school.application.service.ReportCardService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on OrganizationRollupService's 5-arg constructor: flags storing the injected
      OrganizationService (platform application-layer service) and the four school repository
      interfaces into fields. Same standard constructor-injection pattern as
      TenantAdminNotifier/ReportCardService above — investigated 2026-08-17: none of these fields
      hold mutable state that could leak, and none is ever handed back to a caller. Consistent
      detector noise on ordinary DI, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.application.rollup.OrganizationRollupService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on BookController/RouteController's 3-arg constructors: flags storing the
      injected application service and MapStruct mapper references into fields. Same standard
      constructor-injection pattern as every other controller in this module (e.g.
      ClassroomController's 6-arg constructor, never flagged) — investigated 2026-08-23,
      consistent with the TenantAdminNotifier/ReportCardService/OrganizationRollupService findings
      above: this detector flags EI_EXPOSE_REP2 inconsistently across structurally identical
      constructors, which is detector noise, not a real defect — none of these fields hold mutable
      state that could leak, and none is ever handed back to a caller.
    -->
    <Match>
        <Class name="com.altafjava.school.api.controller.BookController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.RouteController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on CirculationService's 4-arg constructor: flags storing the injected
      TenantSettingOverrideService (a platform application-layer service, same category of type as
      TenantAdminNotifier's UserRepository/NotificationService above) into a field. Same standard
      constructor-injection pattern, investigated 2026-08-23: no mutable state leak, reference
      never handed back to a caller — consistent detector noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.application.service.CirculationService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on FeeDefaultRiskRuleEvaluator's 3-arg constructor: flags storing the
      injected UserRepository (a platform interface, same type TenantAdminNotifier already
      constructor-injects above) into a field. Same standard constructor-injection pattern,
      investigated 2026-08-24: no mutable state leak, reference never handed back to a caller —
      consistent detector noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.application.alert.FeeDefaultRiskRuleEvaluator"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on StudentCustomFieldController's 3-arg constructor: flags storing the
      injected CustomFieldValueService (an application service, same category of type as
      BookController/RouteController's injected services above) into a field. Same standard
      constructor-injection pattern, investigated 2026-08-25: no mutable state leak, reference
      never handed back to a caller — consistent detector noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.api.controller.StudentCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on CertificateService's 9-arg constructor: flags storing the injected
      StorageService (a platform interface) into a field — the exact same finding already
      investigated and excluded for ReportCardService above, on the identical StorageService type.
      Investigated 2026-08-25: no mutable state leak, reference never handed back to a caller.
    -->
    <Match>
        <Class name="com.altafjava.school.application.service.CertificateService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on CustomFieldValueService's 2-arg constructor: flags storing the injected
      EntityAttributeService (a platform application-layer interface, same category of type as
      TenantAdminNotifier/CirculationService's injected platform services above) into a field.
      Same standard constructor-injection pattern, investigated 2026-08-25: no mutable state leak,
      reference never handed back to a caller — consistent detector noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.application.service.CustomFieldValueService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on AttendanceController/StudentController's constructors: flags storing the
      injected AttendanceService (an application service, same category of type as
      StudentCustomFieldController/CustomFieldValueController's injected services above) into a
      field. StudentController already injected this exact same AttendanceService reference before
      today's change and was not previously flagged — adding two new methods to AttendanceService
      (updateStatus/delete, for real offline-sync apply/conflict handling) perturbed this
      detector's analysis enough to newly flag it, the same inconsistency-across-structurally-
      identical-constructors already documented for every entry above. Investigated 2026-08-26: no
      mutable state leak, reference never handed back to a caller — consistent detector noise, not
      a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.api.controller.AttendanceController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.StudentController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on AttendanceService/LeaveRequestService's constructors: flags storing the
      newly-injected HolidayService (an application-layer service, same category of type as
      CustomFieldValueService's injected EntityAttributeService above) into a field. Same standard
      constructor-injection pattern, investigated 2026-08-27 (Phase 2, item 2.1): no mutable state
      leak, reference never handed back to a caller — consistent detector noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.application.service.AttendanceService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.application.service.LeaveRequestService"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on HolidayController/TeacherController's constructors: flags storing the
      injected application service references into fields. Same standard constructor-injection
      pattern as every other controller in this module (e.g. BookController/RouteController
      above), investigated 2026-08-27 (Phase 2, item 2.1/2.3): no mutable state leak, reference
      never handed back to a caller — consistent detector noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.api.controller.HolidayController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.TeacherController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on SchoolPlatformConfigurer's constructor: flags storing the injected
      StudentGuardianPiiHandler (an application-layer service, same category of type as
      AttendanceOfflineSyncHandler already constructor-injected here without being flagged) into a
      field. Same standard constructor-injection pattern, investigated 2026-08-28 (Phase 2, item
      2.6): no mutable state leak, reference never handed back to a caller — consistent detector
      noise, not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.config.SchoolPlatformConfigurer"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <!--
      EI_EXPOSE_REP2 on the 8 new custom-field-value controllers' constructors (Phase 3, item
      3.3): flags storing the injected per-entity application service (ClassroomService/
      SubjectService/FeeStructureService/LeaveTypeService/HolidayService/RouteService/
      VehicleService/BookCatalogService) and CustomFieldValueService references into fields. Same
      standard constructor-injection pattern as StudentCustomFieldController above (already
      flagged/excluded for the identical CustomFieldValueService field), investigated 2026-08-29:
      no mutable state leak, reference never handed back to a caller — consistent detector noise,
      not a real defect.
    -->
    <Match>
        <Class name="com.altafjava.school.api.controller.ClassroomCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.SubjectCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.FeeStructureCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.LeaveTypeCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.HolidayCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.RouteCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.VehicleCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
    <Match>
        <Class name="com.altafjava.school.api.controller.BookCustomFieldController"/>
        <Method name="&lt;init&gt;"/>
        <Bug pattern="EI_EXPOSE_REP2"/>
    </Match>
</FindBugsFilter>
