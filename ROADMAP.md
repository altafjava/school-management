# school-saas — Backend Product Architecture & Roadmap

**Scope:** `school-saas` backend only. No frontend content here by design — see `platform-saas/STRATEGIC_ASSESSMENT_AND_ROADMAP.md` at the workspace root for that thread when it's back in scope.
**Method:** grounded in the actual code, verified 2026-08-16 — not the doc-writing "audit" pattern this project has already moved away from (see `platform-saas/.claude/CLAUDE.md` § Documentation Policy). Every claim below cites a real file. Update this doc in place as modules ship; don't supersede it with a new file.

---

## 0. What's actually here today

Confirmed by direct inspection, not by trusting any prior doc:

- **9 domain modules exist as scaffolding, not working features**: `Student`, `Teacher`, `Classroom`, `Attendance`, `AcademicYear`, `Exam`, `Grade`, `FeeStructure`, `FeePayment`. Entities are flat data bags with zero relations (all cross-references are raw `Long` — see §6 for why that's actually the right call). Services do CRUD plus one duplicate-check guard each — no cross-entity validation, no numeric→letter grade computation, no fee balance/outstanding calculation. 5 of 6 scheduler jobs are literal no-op stubs.
- **No Guardian/Parent, Subject, Curriculum, Timetable, Admission, or Enrollment-as-distinct-entity exists anywhere.** Confirmed by grep, not inferred.
- **`platform-saas` already has more than school-saas currently uses.** Multi-campus hierarchy (`Organization` → `Tenant`), feature flags, quota enforcement, an entity-extension pattern, and an opt-in audit aspect are all real, working code — just not yet wired into any school-domain feature. Building school-saas from here means *using* platform capabilities that already exist, not inventing them.

This is the honest starting line the rest of this document builds from.

---

## 1. Complete Backend Feature Set — where each capability belongs

| Capability | Belongs in | Why |
|---|---|---|
| Students, Guardians, Teachers, Staff | `school-saas` | Domain data |
| Academic years, terms, classes/sections, subjects, curriculum | `school-saas` | Domain data |
| Admissions, enrollment | `school-saas` | Domain workflow |
| Attendance, timetable | `school-saas` | Domain workflow |
| Exams, grades, report cards, assignments | `school-saas` | Domain workflow |
| Fee structures, invoices, payments, scholarships/discounts | `school-saas` domain logic **on top of** `platform-saas` billing infra (Stripe integration, invoicing, dunning already exist platform-side) |
| Communication content (what gets sent, to whom, when) | `school-saas` | Domain workflow |
| Notification delivery (email/push/in-app transport) | `platform-saas` (`NotificationService`) — already exists, school-saas just calls it |
| Events (school calendar) | `school-saas` | Domain data |
| Leave management (staff/student) | `school-saas`, P2 |
| Discipline | `school-saas`, P2 |
| Library, transport, inventory/assets | `school-saas`, P2 — genuinely optional modules, build only on real demand |
| Documents/certificates | `school-saas` domain logic **on top of** `platform-saas` `StorageService` (S3/MinIO abstraction already exists) |
| Reporting/analytics | `school-saas` read models **on top of** `platform-saas` observability/metrics infra where it overlaps (e.g. tenant health) |
| Search | `school-saas` indices **on top of** `platform-saas` Elasticsearch infra (tenant-isolated indices, circuit breaker already exist) |
| Import/export | `school-saas` domain-specific format logic; bulk-write mechanics can reuse platform patterns but there's no generic bulk-import framework yet — build the first one in school-saas, extract to platform only if a second domain app needs it (matches how `platform-saas` itself grew: build for real need, generalize on the second consumer) |
| Audit/history | `platform-saas` provides the mechanism (`@Audited` + `AuditAspect`, real but opt-in — see §0); `school-saas` decides *which* mutations need it |
| Multi-tenancy, RBAC infra, feature flags, subscription/billing infra, caching, search infra, observability, file storage | `platform-saas` | Already built, verified real |
| Multi-campus hierarchy | `platform-saas` (`Organization`/`Tenant`) | **Already exists** — see §3, don't rebuild this in school-saas |
| Integrations (SIS export, parent mobile push, third-party gradebook/LMS sync) | External integration, `school-saas`-side adapters later, none now |
| AI-powered capabilities | See §10 — mostly Future |

**What should NOT be built initially:** Library, Transport, Inventory/assets, Discipline, Leave management — real modules, genuinely optional, no evidence of demand yet. A custom RBAC/permission engine, a custom feature-flag system, a custom multi-campus hierarchy, or a custom audit system — all already exist in `platform-saas`; building parallel versions in `school-saas` would be pure waste.

---

## 2. Prioritization

**P0 — Foundation (existing modules must be correct before anything new is built on them)**

- Fix `StudentService.withdraw()` — currently soft-deletes without transitioning `enrollmentStatus` to `WITHDRAWN`; a withdrawn student's status is indistinguishable from an active one that got soft-deleted for another reason.
- Fix `AcademicYearService.create()` — doesn't unset the previous current year when called directly (only the scheduler's rollover job does this correctly); calling the service API directly can produce two "current" years.
- Fix `MarkAttendanceRequest` status parsing — `AttendanceStatus.valueOf()` is called in the controller on unvalidated input; an invalid value throws an unhandled `IllegalArgumentException` instead of a clean 400. Same class of bug is worth auditing across all 9 controllers' enum-typed fields.
- Add application-layer existence validation for every cross-entity `Long` reference (`studentId`, `classroomId`, `examId`, `feeStructureId`, …) before save — right now none of the 9 services check that a referenced ID exists in the tenant. This is a real correctness gap, not a nice-to-have (see §6 for why this belongs at the application layer, not as a JPA `@ManyToOne`).
- Seed `TEACHER`, `PARENT`, `STUDENT` roles (platform's `Roles` class only reserves `SUPER_ADMIN`/`TENANT_ADMIN` — additional roles are meant to be domain data, not platform code; see §3). Every other P0/P1 item assumes these exist.
- Backfill E2E test coverage to the project's own stated minimum (happy path, 401, 403, tenant isolation per controller) — currently 1 of 9 controllers meets it.

**P1 — Core Product**

- ✅ **Done (2026-08-16, Phase 1):** `Guardian`/`Parent` entity + relationship to `Student`. This is the single biggest missing piece — almost every real school workflow (fee responsibility, communication, consent, report access) assumes a guardian exists, and retrofitting a third actor type after building parent-facing features around it is expensive. Model it now, before more is built on `Student` alone.
- ✅ **Done (2026-08-16, Phase 1):** Grade computation: numeric → letter grade, with the grading scale itself tenant-configurable (via `TenantSettingOverride`, already exists platform-side — don't hardcode a single scale).
- ✅ **Done (2026-08-16, Phase 1):** Fee balance / outstanding / partial-payment tracking on `FeePayment`+`FeeStructure` — right now a payment is recorded but nothing ever computes what's owed. A fee module that can't answer "how much does this student still owe" isn't a fee module yet.
- ✅ **Done (2026-08-16, Phase 2):** Implement the 5 stub scheduler jobs for real, using the platform's already-working `NotificationService`: attendance reminders, exam reminders, fee reminders, attendance summary reports, report card generation trigger.
- ✅ **Done (2026-08-16, Phase 2):** `Subject`/`Curriculum` as first-class entities (was a free-text string on `Exam`/`Grade`) — needed before Timetable could exist meaningfully.
- ✅ **Done (2026-08-16, Phase 2):** Timetable (period × classroom × subject × teacher × time slot).
- ✅ **Done (2026-08-16, Phase 3):** Bulk student-roster import (CSV) — high leverage for onboarding a new tenant; a school with 500 existing students isn't going to type them in one by one.
- ✅ **Done (2026-08-16, Phase 3):** Report card generation (PDF) — depended on grade computation being real first (Phase 1).

**P2 — Advanced**

- ✅ **Done (2026-08-16, Phase 3):** Admissions workflow (application → decision → auto-enrollment → guardian linking, which is what establishes billing responsibility in this system's actual design — see Phase 3 below) — a genuine `SagaCoordinator` candidate (the platform's saga infra already exists and was previously unused by school-saas), since it's a real multi-step process with compensable steps.
- Discipline/behavior tracking, leave management, document/certificate generation beyond report cards, advanced reporting dashboards.
- Library, transport, inventory/assets — build only if a real tenant asks.

**P3 — Premium / Differentiating**

- Organization-level rollup reporting — aggregate attendance/fees/enrollment across a school group's campuses, using the `Organization`→`Tenant` hierarchy that already exists platform-side. This is a genuine, realistic differentiator: most school-management SaaS products are single-campus-per-account; this platform can do cross-campus reporting almost for free because the hierarchy is already built.
- ~~Wiring `Plan.featuresJson`/`limitsJson` to actual feature-gating~~ — done (2026-08-16), see §3.
- Parent-facing API surface hardening (rate limits, dedicated auth scope) for an eventual mobile app.

**Future / Experimental**

- AI-assisted report-card comments, attendance-anomaly detection, teacher assistant features, predictive analytics. See §10 — deliberately not ranked higher.

**Feature flags / subscription plans / tenant config — what goes where, using what already exists:**

- Per-tenant module on/off (e.g. "this tenant has Transport enabled"): `FeatureFlagService.isEnabled(key, tenantId)` — real, working API, use it directly. Don't build a second flag mechanism.
- Usage limits (max students, max staff per tenant): `@QuotaCheck` + `QuotaService` — real, working, annotation-driven. Use it on the relevant create endpoints (`StudentController.enroll`, etc.).
- "What does this subscription plan include": **resolved (2026-08-16)** — `platform-saas`'s `PlanEntitlementSyncService` now parses `Plan.featuresJson`/`limitsJson` and materializes them into `TenantFeatureOverride`/`UsageMetric.limitValue` automatically whenever a subscription is activated (`SubscriptionActivatedEvent`) or its plan changes (`SubscriptionPlanChangedEvent` — enables newly granted features, explicitly revokes features lost in a downgrade, replaces limits with the new plan's values). school-saas never needs to hardcode plan-name checks — call `FeatureFlagService.isEnabled(key, tenantId)` / rely on `QuotaService` as normal; entitlements stay in sync with whatever plan the tenant is actually on.
- Tenant-specific custom fields (a school wants to track something school-saas doesn't model): `EntityAttributeService`'s key/value extension pattern — genuinely useful for low-query-frequency custom fields, but it's untyped and unindexed. Don't reach for it as a substitute for a real column when the field needs to be queried, sorted, or validated.

---

## 3. Multi-Tenant SaaS Architecture

**Tenant isolation** is already a platform-enforced boundary, not something school-saas developers need to remember per query — the triple-strategy multi-tenancy (shared/schema/per-tenant-pool) with Hibernate `@Filter` defense-in-depth is verified real and already exercised correctly by every one of the 9 existing school-saas modules (all extend `TenantAwareEntity`/`SoftDeletableEntity`). Nothing to change here — this is the platform working as designed.

**School/campus hierarchy — already solved, use it, don't rebuild it.** `Organization` (name, slug, contact/billing email, `maxTenants` quota) owns multiple `Tenant`s via `Tenant.organizationId`, with `OrganizationMembership` giving org-level admins visibility across their tenants. Map this directly: **one school group = one Organization; each campus = one Tenant under it.** This answers the "multiple campuses" requirement from the platform layer up — school-saas's job is to build the rollup reporting on top of it (§2, P3), not to invent its own hierarchy.

**RBAC — real gap, needs closing at P0.** Only two roles are wired end-to-end today: `SUPER_ADMIN`, `TENANT_ADMIN`. A `Role` entity and a `Permission` enum (~20 fine-grained values) exist but are completely unreferenced outside their own definitions — no enforcement path uses them. `ResourceAccessPolicy` is the real, working extension point (school-saas already implements it in `SchoolResourceAccessPolicy`, restricting a teacher's classroom `READ` access to their own assignment) — this is the right pattern, extend it, don't replace it. What's missing is the role layer underneath it: seed `TEACHER`, `PARENT`, `STUDENT` as `Role` rows via school-saas's own migration (roles are data, not platform compile-time constants — `Roles.java`'s two constants are the platform's own reserved names, not an exhaustive list), then gate controllers with `@PreAuthorize("hasRole('TEACHER')")` etc., and extend `SchoolResourceAccessPolicy` per new resource type as needed (e.g. a parent can only `READ` their own child's records — **done, Phase 1**: `SchoolResourceAccessPolicy`'s `STUDENT`/`READ` rule, enforced via `StudentDataAccessGuard` on the grades/attendance/fee-balance endpoints).

**Feature flags, entitlements, audit, caching, background jobs, notifications, search** — all real platform capabilities; school-saas's job is to *call* them correctly, not build parallel versions. See §2 for the flag/quota/plan-gating specifics and §0/§8 for the audit-trail specifics.

---

## 4. Domain Architecture — Module Boundaries

**Not just 9 modules.** Today's 9 are a subset. Near-term target is ~10 core modules (table below); with P2/P3 modules (Discipline, Leave, Library, Transport, Inventory — §2) added only on real demand, the eventual count is 15+. The 9 existing ones aren't wrong, they're incomplete — no module here needs to be deleted, several new ones need to be added alongside them.

Keep the modular monolith the codebase already uses (`domain/{context}/model`, `application/{context}/service`, `api/controller` — this is already the actual layout, not a proposal). No microservices split — nothing here has an independent scaling or team-ownership justification yet, and splitting now would be pure speculation against a future that isn't here.

| Module | Responsibility | Owns | Depends on (read-only, via ID) | Key events |
|---|---|---|---|---|
| **Students** | Student identity, enrollment status | `Student` | — | `StudentEnrolledEvent`, `StudentWithdrawnEvent` |
| **Guardians** ✅ *(built, Phase 1)* | Guardian identity, guardian↔student links, consent | `Guardian`, `StudentGuardianLink` | Student (by ID) | `GuardianLinkedEvent` |
| **Staff** | Teacher/staff identity | `Teacher` | — | — |
| **Academics** | Academic years, terms/semesters, classes/sections, subjects, curriculum | `AcademicYear`, `Term` *(new)*, `Classroom`, `Subject` *(new)* | Teacher (by ID, for class-teacher assignment) | `AcademicYearRolledOverEvent` |
| **Admissions** ✅ *(built, Phase 3)* | Application intake → decision → enrollment handoff | `Admission`, `AdmissionDecision` | Student (creates one on approval) | `AdmissionApprovedEvent`(*planned name — implemented as the saga's own step log, not yet a published domain event; see Phase 3 writeup*) |
| **Attendance** | Daily attendance records | `Attendance` | Student, Classroom (by ID) | `AttendanceMarkedEvent` |
| **Timetable** *(new, P1)* | Period × classroom × subject × teacher scheduling | `Period`, `TimetableEntry` | Classroom, Subject, Teacher (by ID) | — |
| **Examinations** ✅ *(ReportCard built, Phase 3)* | Exams, grades, report cards | `Exam`, `Grade`, `ReportCard` | Student, Classroom, Subject, Term (by ID) | `GradeRecordedEvent`, `ReportCardGeneratedEvent` |
| **Fees** | Fee structures, payments, balances, scholarships | `FeeStructure`, `FeePayment`, `Scholarship` *(new, P2)* | Student, Guardian, Term (by ID, billing responsibility/cycle) | `FeePaymentRecordedEvent`, `FeeOverdueEvent` |
| **Communication** *(new, P1-P2)* | What gets sent to whom, when — templates and triggers | `CommunicationTemplate`, `CommunicationLog` | Student, Guardian, Teacher (by ID); calls platform `NotificationService` for delivery | — |

**New gap found in this pass: `Term`/`Semester`.** `AcademicYear` currently has no sub-division — real schools run fee cycles, report cards, and often exams per term/semester, not per year. Add `Term` (belongs to one `AcademicYear`, has its own start/end) as part of the Academics module in Phase 1, before Fees/Examinations need a cycle boundary to attach to.

**Two design decisions worth stating explicitly, not leaving implicit** (given the instinct to distrust the current design — these were deliberate, re-examined now, and confirmed rather than overlooked):

- **`Classroom` stays as one entity with `grade`/`section` string fields, not split into separate `Grade`/`Section` entities — for now.** Splitting is the right move only once a section needs its own capacity/roster/teacher-assignment lifecycle independent of its classroom (e.g., multiple sections per grade with different capacities and class teachers, which today's model can't express well). No evidence of that need yet. If/when it arrives, do it then — the fields are contained enough that this isn't a costly deferral, and building it now against no real requirement would be exactly the speculative over-building the brief warns against.
- **Cross-module references stay raw `Long` IDs, not JPA `@ManyToOne`** (see §6) — this is the correct DDD module-boundary pattern, confirmed by platform-saas's own convention (114+9 entities, only one `@OneToMany`/`@ManyToMany` in the entire platform). The gap isn't the reference style, it's that nothing validates the ID exists (§2 P0) — fixed at the application layer, not by changing the reference style.

Every cross-module reference is by ID. Events cross module boundaries; direct entity/repository access across modules does not.

---

## 5. Platform vs School Responsibilities — corrections found

Reviewing the actual code against this split surfaced two things already in the wrong place, and one thing correctly placed that's worth calling out so it isn't "fixed" by mistake:

- **Correctly placed, don't touch:** `SchoolResourceAccessPolicy` living in school-saas, implementing platform's `ResourceAccessPolicy` SPI. This is the extension point working exactly as designed — resist any urge to move classroom-level access logic into the platform.
- **~~Gap in platform-saas~~ — resolved (2026-08-16):** `Plan.featuresJson`/`limitsJson` are now wired end-to-end via `PlanEntitlementSyncService` (see §2). No school-saas workaround needed.
- **Gap in platform-saas, not urgent:** RBAC's `Permission` enum and `Role.permissionsJson` are modeled but never enforced anywhere. If school-saas eventually needs finer-grained permissions than role-based `@PreAuthorize` (e.g. "can view grades but not edit them"), that enforcement plumbing needs to be built in `platform-saas` (it's the natural home for a permission-check mechanism), with school-saas only defining which permissions its roles carry.

---

## 6. Database & Data Architecture

**Tenant strategy:** stay on `SHARED` (row-level `tenant_id`) for school-saas tenants — nothing about school data volume or isolation requirements justifies `SCHEMA` per-tenant at this stage, and the platform's Hibernate `@Filter` defense-in-depth already makes `SHARED` genuinely safe. Revisit only if a specific enterprise customer contractually requires physical schema isolation.

**The "raw `Long` instead of `@ManyToOne`" pattern across all 9 entities is correct, not a shortcut to fix.** It's the right way to reference across aggregate/module boundaries in a modular monolith — it's what keeps modules genuinely separable later if that's ever justified, and it's consistent with platform-saas's own module-boundary discipline. **The actual gap is that nothing validates the referenced ID exists before save** (§2, P0) — that's an application-layer job (a lookup call to the owning module's repository/service), not a reason to introduce JPA object references.

**Indexes:** every new table needs `tenant_id`, `status`/`deleted`, `created_at`, and all FK-shaped `Long` columns indexed — matches `platform-saas`'s own hard rule and its own hard-won lesson (the platform's `PLATFORM_READINESS_ASSESSMENT` audit found and fixed exactly this class of gap on platform tables; don't repeat it here).

**Historical academic data / archiving / partitioning:** not yet. A school's attendance+grade history for even a few thousand students over several years is small by any modern database's standards — partitioning is a technique for a problem this system doesn't have yet. Revisit only when a real tenant's data volume or a real reporting-latency complaint provides the trigger, not preemptively.

**Bulk operations:** the one place this matters now is student-roster import (§2, P1) — design that as a real bulk-insert path (batched, validated, reported per-row failure) from the start, since retrofitting bulk semantics onto a one-row-at-a-time API later is genuinely painful.

---

## 7. Performance & Scalability

Nothing here requires a decision "now to avoid an expensive rewrite later" beyond what's already covered: keep FK-by-ID (§6), keep indexes disciplined (§6), use the platform's paginated list-endpoint convention (already followed by all 9 controllers), use the platform's bounded async executor for non-critical writes (notifications, audit — already the platform default). At current and realistically near-term scale (a handful of tenants, thousands of students each), the platform's existing caching (Caffeine + Redis), connection pooling, and circuit breakers are more than sufficient. Horizontal scaling of the application tier is a deployment-pipeline concern (there isn't one yet — see the root `STRATEGIC_ASSESSMENT_AND_ROADMAP.md`), not a school-saas code concern.

---

## 8. Security & Reliability

- **PII**: `@Pii` is already used on `Student.email`/`Teacher.email` — extend it to `Guardian` fields once that entity exists (phone, address), and audit whether `dateOfBirth` should carry it too.
- **Audit trail — concrete task list, since it's opt-in not automatic (§0):** annotate with `@Audited`: grade changes, fee payment recording, attendance corrections (not the initial mark — the correction), enrollment status transitions. These are the actions a school will actually need to answer "who changed this and when" about.
- **Idempotency:** `FeePaymentService.record()` currently only blocks a duplicate receipt number — tighten this to a real idempotency-key pattern (the platform already has one, per its own README) once payments carry real money-movement consequences, not just record-keeping.
- **Rate limiting, retries, timeouts, circuit breakers:** inherited from the platform for anything going through its HTTP/async infra — nothing school-saas-specific needed here yet.
- **Backup/recovery:** genuinely not applicable yet — there's no production deployment (see root roadmap). Don't write a school-saas-specific DR doc before platform-saas has a real one to inherit from.

---

## 9. API & Integration Architecture

Already-compliant patterns to keep: DTO records (never JPA entities returned), pagination on every list endpoint, `@Valid` request DTOs, `publicId` exposure. Two concrete additions:

- **Bulk import API** for student rosters (§2/§6) — the first real bulk endpoint in school-saas; design its response shape (per-row success/failure) once and reuse the pattern for future bulk needs (bulk grade entry, bulk fee-structure assignment) rather than inventing a new shape each time.
- **Webhooks** — not yet. No external consumer exists to deliver to. Design the outbound event shape (already Java records, IDs/primitives only per platform convention) so adding webhook delivery later is additive, not a rework.

Public vs internal API distinction isn't a real question yet — everything is internal to the one deployed app; revisit when/if a public integration API is actually requested.

---

## 10. Automation & AI — mostly Future, deliberately

Per the platform's own stated principle, don't add AI because it's fashionable. Ranked honestly:

- **Postpone entirely for now:** attendance-anomaly detection, predictive analytics, intelligent search, teacher assistant features. No workflow they'd sit on top of is real yet (§0) — there's nothing to be intelligent about until grades/attendance/fees are genuinely computed, not stubbed.
- **Design for later, don't build now:** automated communication drafting (e.g. a suggested report-card comment from a grade trend) — plausible once report cards (P1) and the Communication module (P1/P2) are both real; not before.
- **Build now:** nothing. This is the correct answer at this stage, not a gap.

---

## 11. Engineering Principles

Inherit `platform-saas`'s `.claude/CLAUDE.md` in full — same group of standards already governs school-saas (module layering, service-layer rules, testing tiers, security rules, hard rules). Additions specific to what this review surfaced:

- **Model actor types (Guardian, Teacher, Student-as-user) as first-class from the start** — retrofitting a third actor type after building features around a two-actor assumption is the single most expensive mistake available here; §2 P1 exists specifically to avoid it.
- **Cross-module references by ID, always** (§6) — already the pattern, keep it as modules grow.
- **Application-layer existence validation is not optional** (§2 P0) — a flat, FK-by-ID design only stays safe if every write path checks what it's referencing.
- **Use platform capabilities before building parallel ones** — feature flags, quotas, audit, entity extension, saga coordination, resource-access policy all already exist; the temptation to build a school-saas-local version of any of these should be treated as a bug in the plan, not a reasonable choice.

**Actively avoid:** premature microservices split, a school-saas-local RBAC/flag/audit system, God services (keep the one-guard-clause-per-service discipline as real logic gets added — split before a service exceeds ~5 responsibilities), building Library/Transport/Inventory/Discipline before real demand, a saga for anything that isn't genuinely multi-step-with-compensation (Admissions qualifies; most CRUD does not), scattering `FeatureFlagService.isEnabled()` calls through business logic instead of checking once at the boundary (controller or a dedicated gating service).

---

## 12. Competitive Differentiation — realistic for a solo developer

- **Multi-campus rollup reporting** (§2 P3) — the platform's `Organization` hierarchy makes this close to free to build here and genuinely rare in typical school-management SaaS, which is usually single-campus-per-account.
- **API-first design** — already true by construction (versioned REST API, OpenAPI/springdoc live, DTO contracts stable) — worth stating as a real strength when it eventually matters for third-party integrations (LMS, SIS sync), not a thing to build, a thing to not accidentally break.
- **Configurability without redeploy** — feature flags + tenant settings + entity extension already give school-saas a real edge over school ERPs that require a vendor ticket to change a grading scale or add a custom field.
- **Not now:** AI-driven differentiation (§10) — real, but premature; the honest differentiator today is architectural soundness, not intelligence features nothing is built to receive yet.

---

## Development Roadmap

Testing/CI gates below are defined in detail in `TESTING.md` — this section states which gate must be real by the end of each phase, not how it works.

**Phase 0 — Harden what exists — ✅ COMPLETE (2026-08-16).** `./gradlew clean build` green: 236/236 tests, `spotlessCheck` blocking, all 4 ArchUnit fitness tests blocking, OpenAPI snapshot test blocking, JaCoCo reporting live per module.

- ✅ All 3 correctness bugs fixed: `StudentService.withdraw()` now transitions `enrollmentStatus` to `WITHDRAWN`; `AcademicYearService.create()` now unsets the previous current year; `MarkAttendanceRequest.status` is a real `AttendanceStatus` enum (was `String` + unchecked `.valueOf()` — an invalid value now gets a clean 400 via Jackson/`HttpMessageNotReadableException`, not an unhandled 500).
- ✅ Cross-entity existence validation added to the 5 services that reference another module's entity by ID (Classroom→Teacher, Attendance→Student/Classroom, Exam→Classroom, Grade→Student/Exam, FeePayment→Student/FeeStructure) — each throws `ResourceNotFoundException` before the write, rather than letting the database's existing FK constraint reject it with a raw `DataIntegrityViolationException`. The FK constraints themselves were already there (real referential integrity was never actually at risk) — this closes the error-experience gap, not a data-corruption one.
- ✅ **TEACHER/PARENT/STUDENT roles were already seeded** — found already present in `platform-saas`'s own `data/seed-tenant-roles.xml` (per-tenant, alongside `TENANT_ADMIN`/`PRINCIPAL`), confirmed by direct inspection. No school-saas migration needed; this line item is satisfied by platform-side work already done.
- ✅ `Term` module built as a full vertical slice (entity, repository, service, controller, DTOs, mapper, Liquibase migration `003-terms.xml`), matching every existing module's pattern exactly.
- ✅ Tenant-isolation integration tests added for all 8 modules that lacked them (Teacher, Classroom, AcademicYear, Exam, Grade, FeeStructure, FeePayment, Term) — 39 integration tests total, up from 2.
- ✅ E2E tests added for all 9 controllers that lacked them (the 8 above + Attendance) — 41 E2E tests total, up from 4, each covering the full CLAUDE.md minimum (happy path, unauthenticated → 401, wrong role → 403, cross-tenant → 404).
- ✅ Unit tests added for all 7 changed services + `TermService` — 25 unit tests, up from 0 (there was no domain-logic unit test coverage in school-saas before this phase).

**Two real bugs found and fixed along the way, neither originally on this list:**
- **`SchoolTenantProvisioningListener` silently failed to seed every new tenant's default academic year.** Bare `@Async` (not `@Async("platformTaskExecutor")`) ran on an executor with no tenant-context-propagating `TaskDecorator`, and — more fundamentally — there was no tenant context to propagate anyway, since a brand-new tenant is never "current" on any request thread. Fixed by running the handler body through `TenantContext.runAsTenant(...)`, the platform's own sanctioned API for exactly this case (see `AbstractBaseJob`'s use of it), binding the tenant from the event payload instead of relying on ambient context. Also switched `@TransactionalEventListener(AFTER_COMMIT)` → plain `@EventListener`, matching platform-saas's own documented reasoning for `TenantCreatedEvent` specifically (outbox-routed, so a `@TransactionalEventListener` isn't guaranteed to fire).
- **A real client can never populate `classTeacherId`/`classroomId`/`studentId`/`examId`/`feeStructureId` on any create request.** Response DTOs correctly expose only `publicId` (per CLAUDE.md's DTO rule), but the corresponding request DTOs across 5 modules require the raw internal `Long` ID — which no GET endpoint ever returns. Not fixed in this phase (would mean touching 5 modules' request DTOs, services, and controllers, and Phase 0's own scope was already large) — worked around in the new E2E tests via direct repository lookup, with the gap documented in each test's class Javadoc. **Flagging this as a real, not-yet-scheduled fix**: request DTOs for cross-entity references should accept the referenced entity's public UUID and resolve it to the internal ID server-side, the same direction the response side already went.

**Phase 1 — Make the core academic + fee workflow real — ✅ COMPLETE (2026-08-16).** Guardian entity + student links, grade computation with tenant-configurable scale, fee balance/partial-payment tracking.

- ✅ `Guardian` + `StudentGuardianLink` entities (`domain/guardian/`), Liquibase `004-guardians.xml`; `guardians.user_id`/`students.user_id` nullable FKs to platform `users` — a guardian or student may optionally hold a login account with role `PARENT`/`STUDENT`. `GuardianService`: create, link to student (`RelationshipType`: MOTHER/FATHER/LEGAL_GUARDIAN/OTHER, `primaryContact`, `consentGivenAt`), publishes `GuardianLinkedEvent`. `GuardianController`: full CRUD + `POST /api/v1/guardians/{publicId}/students` (link) + `GET /api/v1/guardians/me/students` (parent self-service, returns real `StudentResponse`s with `publicId` — not the raw internal ID gap flagged in Phase 0).
- ✅ Grade computation is real: `GradeCalculator` (pure domain service, `domain/grade/service/`) converts marks + `Exam.maxMarks` into a letter grade against a `GradingScale` — no longer client-supplied (`RecordGradeRequest.gradeLetter` removed). `GradingScale`/`GradeThreshold` are tenant-configurable via `GradingScaleService`, which bridges to the platform's `TenantSettingOverrideService` (JSON under key `school.grading.scale`); tenants without an override get `GradingScale.defaultScale()` (A/B/C/D/F at 90/80/70/60/0). `GradingScaleController`: `GET`/`PUT /api/v1/grading-scale` (TENANT_ADMIN).
- ✅ Fee balance is real: `FeeBalanceCalculator` (pure domain service, `domain/fee/service/`) computes `amountDue`/`amountPaid`/`outstandingBalance`/`overpaidAmount` per fee structure from the sum of a student's payments — overpayment is reported explicitly, never silently negative. Every `FeeStructure` in a tenant applies to every student (documented assumption — no fee-structure-to-student assignment table exists yet); narrowing this later doesn't change the calculation itself. Exposed at `GET /api/v1/students/{publicId}/fee-balance`.
- ✅ Ownership-based RBAC, not just role-based: `SchoolResourceAccessPolicy` gained a `STUDENT`/`READ` rule (self or linked guardian, resolved via real `users.id` — not the classroom policy's pre-existing teacher-ID/user-ID conflation, which Phase 1 left untouched as out of scope). `StudentDataAccessGuard` (`application/security/`) enforces it on three new nested read endpoints — `GET /api/v1/students/{publicId}/grades`, `/attendance`, `/fee-balance` — TENANT_ADMIN/TEACHER bypass, PARENT/STUDENT must own the record or get a real 403 (`AccessDeniedException`, not the `ResourceAccessPolicyEnforcer.assertAllowed` 400 path, which doesn't match Spring Security's expected semantics for a denial). `fee-balance` excludes TEACHER entirely, matching TESTING.md's RBAC matrix (§1) exactly.
- ✅ **Testing/CI work (`TESTING.md` §5, steps 5–7) — all landed, all blocking, no aspirational thresholds:**
  - JaCoCo coverage ratchet: `domain` floor 31%, `application` floor 47% (both measured, not guessed) via `jacocoTestCoverageVerification` wired into `check`.
  - `:domain:pitest` wired into CI and blocking at the existing `mutationThreshold = 70`/`coverageThreshold = 70` — required bumping `pitest-core` to 1.25.9 and the `gradle-pitest-plugin` to 1.19.0 (the versions pinned since Phase 0 predate Java 25 / ASM 9.8 support and crashed outright reading this project's class files) and narrowing `targetClasses` from the whole `domain` package down to `grade.service`/`grade.model` (`GradingScale`/`GradeThreshold` only, not the `Grade` entity)/`fee.service` — the packages Phase 1 actually put real logic in. Real result: 97% line coverage, 88% mutation score on that scope. A genuine, separate tooling gap surfaced and is documented in `domain/build.gradle`: the plugin has no Java toolchain support, so its subprocess needs the *Gradle daemon itself* on JDK 25, not just the project's toolchain — `ci.yml` handles this with a scoped `actions/setup-java` step immediately before the PIT step.
  - SpotBugs added — zero-findings baseline achieved by fixing all 6 real findings at the source (immutable list defensive copies on `GradingScale`/`UpdateGradingScaleRequest`/`GradingScaleResponse`, a double `SecurityContextHolder.getAuthentication()` call in `GuardianService`), not by suppressing them; `config/spotbugs/exclude.xml` stays empty.
  - Full RBAC E2E matrix (§1 table): `StudentDataAccessE2ETest` covers TENANT_ADMIN (any student), PARENT (own child ✅ / other child ❌ 403), STUDENT (own ✅ / other ❌ 403), TEACHER excluded from fee-balance — using real platform `User` rows (not the synthetic `user_id=-1` token used elsewhere for plain role checks), since ownership assertions need a real guardian/student-to-user link to prove anything.
- **Dependencies:** Phase 0 (roles must exist before guardian-scoped access control makes sense) — met.
- **Outcome:** a parent (once a frontend exists) can genuinely see their child's real grade and real fee balance — the numbers are correct, not placeholder.
- **Validated:** `GuardianTenantIsolationIntegrationTest` + `SchoolResourceAccessPolicyIntegrationTest` prove a guardian can only see their own linked student's data (and not via any listing endpoint either); PIT mutation score on `GradeCalculator`/`FeeBalanceCalculator` (88%) meets threshold, not just line coverage.
- **Deferred, not silently dropped:** teacher-scoped "own classroom only" filtering on grade/attendance view (TESTING.md's matrix column) — TEACHER keeps the tenant-wide read access it already had from Phase 0; narrowing it to a teacher's own classroom requires walking the Grade→Exam→Classroom / Attendance→Classroom chain and re-touching existing, already-tested endpoints, which was judged a larger, separate change from Guardian/grade-computation/fee-balance. **Still open after Phase 2** — Phase 2's scope was scheduling/notifications/Subject/Timetable, not RBAC, so this wasn't touched; retargeted to Phase 3 (report-card generation touches the same Grade→Exam→Classroom chain, so it's a natural point to revisit this together rather than as an isolated change).

**Phase 2 — Make scheduling and notifications real — ✅ COMPLETE (2026-08-16).** Implement the 5 stub scheduler jobs for real against `NotificationService`. Subject/Curriculum as first-class entities. Timetable module.

- ✅ `Subject` first-class entity (`domain/subject/`, Liquibase `002-school-extended-tables.xml`'s new `school-005a-subjects` changeset) — `Exam.subject`/`Grade.subject` (free-text `String`) replaced with `subjectId` (`Long`, FK-validated at the service layer, same by-ID convention as every other cross-module reference in this codebase). `Grade.subjectId` is now *derived* from `Exam.subjectId`, not client-supplied — closes a latent data-integrity gap the old free-text design had (a grade's subject could previously disagree with its exam's subject; now that's structurally impossible). `SubjectService`/`SubjectController`: list, get, create, deactivate (TENANT_ADMIN write, TENANT_ADMIN/TEACHER read).
- ✅ Timetable module (`domain/timetable/`, Liquibase `005-timetable.xml`): `Period` (name, start/end time, display order) and `TimetableEntry` (day of week × period × classroom × subject × teacher). `TimetableService.schedule()` validates all four referenced IDs exist, then rejects double-booking a classroom or a teacher for the same day/period — enforced at both the service layer (checked before insert) and the database (`uq_timetable_classroom_period_day`/`uq_timetable_teacher_period_day` unique constraints, so the invariant holds even under concurrent requests, not just under this service's own check-then-act). `PeriodController`/`TimetableController`, plus `GET /api/v1/classrooms/{publicId}/timetable`.
- ✅ All 5 scheduler jobs now do real work against real school data and the platform's real `NotificationService`, not stub logging:
  - `FeePaymentReminderJob` — every ACTIVE student with an outstanding balance (via `FeePaymentService.calculateBalanceForStudent`, the same calculator Phase 1 built) gets a `FEE_DUE` notification with the real amount owed.
  - `ExamScheduleReminderJob` — every exam in the next 2 days notifies every student in that exam's classroom with an `EXAM_SCHEDULED` notification naming the real subject.
  - `DailyAttendanceReminderJob` — every classroom with no attendance marked yet today reminds its class teacher (if the teacher has a login account).
  - `AttendanceSummaryReportJob` — computes real weekly PRESENT/ABSENT/LATE/EXCUSED counts and notifies every tenant admin with the real numbers.
  - `ReportCardGenerationJob` — notifies tenant admins with the real count of active students eligible for a report card. PDF generation itself is explicitly out of scope here (Phase 3) — this job's job is the real, verifiable trigger signal, not the document.
  - New support classes closing two real gaps these jobs exposed: `StudentNotificationRecipientResolver` (primary guardian → any linked guardian → the student's own account → nobody, in that order) and `TenantAdminNotifier` (queries real `TENANT_ADMIN` users via `UserRepository`, not a hardcoded list). `Teacher.userId` (nullable FK to platform `users`, same pattern as Phase 1's `Student.userId`/`Guardian.userId`) was added because `DailyAttendanceReminderJob` cannot resolve a recipient without it — necessary, not scope creep.
  - Classroom rosters for `ExamScheduleReminderJob` are inferred from `Attendance` history (distinct student IDs per classroom) — the same honest, documented workaround Phase 1 used for fee structures, since no student-to-classroom enrollment table exists yet (`AttendanceRepository.findDistinctStudentIdsByClassroomId`).
- ✅ **A real bug found and fixed along the way, not originally on this list:** `IllegalArgumentException` thrown from a service was never mapped by `GlobalExceptionHandler` (platform-saas) — it fell through to the generic `Exception` handler and returned 500 instead of 400, for every one of the ~10 pre-existing services in this codebase that throw it for validation failures (duplicate codes, business-rule conflicts). Surfaced by `TimetableCrudE2ETest`'s classroom-double-booking test, which genuinely failed with 500 before the fix. Fixed narrowly, in scope: `TimetableService`/`SubjectService`/`PeriodService` (the three Phase 2 added) now throw `BusinessException` instead, which `GlobalExceptionHandler` already correctly maps to 400 — the same exception type `GradingScaleService` already used. The ~10 pre-existing `IllegalArgumentException` sites elsewhere in the codebase (`StudentService`, `GuardianService`, `TeacherService`, `FeeStructureService`, etc.) were left untouched — real, but out of Phase 2's scope; flagging it here rather than letting it go unrecorded.
- ✅ **Testing/CI work — all landed, all blocking, no aspirational thresholds:** 297 tests total across all modules (up from Phase 1's 244), all passing — 65 E2E (14 new: `SubjectCrudE2ETest`, `PeriodCrudE2ETest`, `TimetableCrudE2ETest`, each at the full CLAUDE.md minimum, including a classroom-double-booking → 400 case), 59 tenant-isolation/persistence integration tests (11 new: Subject/Period/Timetable tenant isolation, plus `ReportCardGenerationJobIntegrationTest` — the literal implementation of this phase's "Validate" requirement, running the real job against a real Spring context and asserting on a real, persisted `Notification` row's tenant/type/title/message content, not a mock), 67 `application` unit tests (27 new: 5 scheduler-job test classes using `ArgumentCaptor<SendNotificationCommand>` to assert exact recipient/type/message content, plus `TimetableServiceTest`/`SubjectServiceTest`/`PeriodServiceTest`), 20 `domain` unit tests (4 new: `SubjectTest`/`PeriodTest`/`TimetableEntryTest`).
  - JaCoCo coverage ratchet **raised, not just held**: `domain` floor 31% → 41% (Subject/Period/TimetableEntry gained direct domain-level tests), `application` floor 47% → 62% (the 5 scheduler jobs' real logic is now real tested logic, not stub logging with nothing to assert on).
  - SpotBugs zero-findings baseline held — one new finding investigated (`EI_EXPOSE_REP2` on `TenantAdminNotifier`'s constructor, flagging a stored `UserRepository` reference), confirmed to be detector noise on a standard, safe constructor-injection pattern already used elsewhere in this codebase without being flagged (e.g. `GuardianService`'s `EventPublisher` field) — documented and narrowly excluded in `config/spotbugs/exclude.xml`, which otherwise stays as empty as it was at the end of Phase 1.
  - `:domain:pitest` scope intentionally unchanged — `TimetableService`'s conflict-validation logic (the one piece of genuinely new business logic this phase added) lives in `application`, not `domain`, so it's out of PIT's current `domain`-only scope. Not a gap worth closing yet: PIT has no toolchain support and needs its own JDK 25 host-JVM wiring (see `domain/build.gradle`), and `application` doesn't have PIT wired up at all — a real, separate piece of work, not something to bolt on incidentally here.
  - OpenAPI snapshot regenerated for the 3 new controllers + the `subjectId` breaking change on `ExamController`/`GradeController`'s request/response shape (both already used real client references, not response-only fields, so this wasn't a versioned `/api/v2/` change — no external client existed to break).
- **Dependencies:** Phase 1 (notification content needs real grade/fee/attendance data to reference) — met.
- **Outcome:** the platform's scheduling and notification infrastructure is genuinely exercised by school-domain logic for the first time — every job now reads real data and calls the real `NotificationService`, not stub logging that merely proved the job registers and runs at Spring context startup.
- **Validated:** `ReportCardGenerationJobIntegrationTest` runs the real job in a real Spring context against a real tenant/student, then asserts on a real, persisted `Notification` row's tenant ID, type, title, and message content — the literal "a scheduled job actually sends a real notification in a test environment and it's verifiably correct content" requirement, not a paraphrase of it. Coverage ratchet raised on both `domain` and `application`, not just maintained by omission.
- **Deferred, not silently dropped:** bulk student-roster import and report card PDF generation (both already scoped to Phase 3, unaffected); teacher-scoped classroom filtering on grade/attendance view (Phase 1's deferred item, still open — see above, retargeted to Phase 3).

**Phase 3 — Onboarding and output — ✅ COMPLETE (2026-08-16).** Admissions workflow (as a saga), bulk student-roster import, report card PDF generation.

- ✅ Admissions workflow, built as a genuine multi-step saga, not a single transaction wearing a saga's name: `Admission`/`AdmissionDecision` entities (`domain/admission/`, Liquibase `006-admissions.xml`), `AdmissionService` (submit → markUnderReview → decide), and `AdmissionEnrollmentSaga` (`application/saga/`, extends the platform's `SagaCoordinator`) with 4 real steps — ENROLL_STUDENT, LINK_GUARDIAN (the mechanism that establishes fee/billing responsibility in this system's design, since there is no separate `BillingAccount` entity), NOTIFY, MARK_ENROLLED. `POST/PATCH /api/v1/admissions/...` (TENANT_ADMIN — this is staff processing applications, not a public intake form; §9 already noted a public API surface isn't a real question yet).
  - A saga step's whole point is to be independently compensable, which means each step's write must be durably committed before the next runs, not held open in one long-lived transaction — `AdmissionService.decide()`/`AdmissionEnrollmentSaga.enroll()` are deliberately **not** `@Transactional`, letting each repository/service call commit on its own. Getting this right the first time surfaced (and this is exactly what the integration test tier is for — see Validated below) a genuine `StaleObjectStateException`: reusing the pre-save in-memory `Admission` reference across multiple independently-committing saves trips Hibernate's optimistic lock, since the DB's `@Version` has already moved past what the stale reference holds. Fixed by reassigning `admission = admissionRepository.save(admission)` after every write, not by adding a transaction back.
- ✅ Bulk student-roster CSV import — the first real bulk-write path in school-saas (§9): `StudentBulkImportService` (Apache Commons CSV), one row = one call to `StudentService.enroll` (the same validated, tested single-enrollment entry point), each row's outcome independent — a bad row (duplicate code, malformed date, missing field) is reported and skipped, never aborts the file. `POST /api/v1/students/bulk-import` (multipart, TENANT_ADMIN), response shape is per-row (`totalRows`/`successCount`/`failureCount`/`failures[]`) — the reusable shape §9 asked for future bulk endpoints to share.
  - **Not `@QuotaCheck`-gated, and that's a deliberate finding, not an oversight:** `QuotaService.getEffectiveLimit` treats a metric with no `UsageMetric` row configured as limit `0` (blocked), not unlimited — confirmed by a genuinely failing integration test the first time `@QuotaCheck(metric = "BULK_IMPORTS")` was wired in. Since no plan has ever needed a BULK_IMPORTS limit before this endpoint existed, gating on it would reject every tenant outright on day one, not rate-limit real overuse. The real fix (seeding a default per-plan limit) is platform data work, out of this phase's scope — flagged here rather than silently shipping a check that breaks the feature it's attached to.
- ✅ Report card PDF generation — `ReportCard` entity (`domain/reportcard/`, Liquibase `007-report-cards.xml`), `ReportCardService.generate(studentId, termId)` aggregates every `Grade` whose `Exam` falls within the `Term`'s date range (no `termId` column on `Exam` — the term boundary is a date-range filter against `Term.startDate`/`endDate` instead, avoiding a schema change to an entity two earlier phases already shipped and tested), renders a real PDF via OpenPDF (`ReportCardPdfGenerator`, already in the dependency catalog, unused until now), and stores it via the platform's `StorageService`. Regenerating soft-deletes the previous report card for that student/term rather than erroring. `ReportCardGenerationJob` (Phase 2's stub, explicitly deferred then) now actually generates PDFs for every active student against whichever `Term` contains today, then notifies admins with the real count — completing what Phase 2's javadoc promised. `GET/POST /api/v1/students/{publicId}/report-cards`, `GET .../report-cards/{id}/download` — same `StudentDataAccessGuard` ownership pattern Phase 1 built for grades/attendance/fee-balance, ownership-checking reused, not reinvented.
- ✅ **Teacher-scoped "own classroom only" filtering — the item deferred since Phase 1, retargeted here in Phase 2's update — is done:** `TeacherClassroomScopeResolver` bridges the authenticated platform user to their `Teacher` record (via `Teacher.userId`, added Phase 2) and then to the classrooms they're assigned class teacher of; `GradeService.listGrades`/`AttendanceService.listAttendance` now narrow to those classrooms for TEACHER callers, unchanged (tenant-wide) for TENANT_ADMIN. Deliberately fails **closed**: a TEACHER-role caller with no resolvable `Teacher` link gets an empty result, not unrestricted access — a real gap caught while writing `TeacherClassroomScopeResolverTest`, before it ever reached a real tenant.
  - **A second real, pre-existing bug found and fixed in the same pass:** `SchoolResourceAccessPolicy.isTeacherAssignedToClassroom` compared the caller's platform `userId` directly against `classroom.classTeacherId` — a `Teacher.id`, not a `users.id` — the exact "teacher-ID/user-ID conflation" Phase 1 documented and explicitly left untouched as out of scope. Fixed using the same `Teacher.userId` bridge. The one pre-existing integration test covering this path (`SchoolResourceAccessPolicyIntegrationTest.teacher_canAccessOwnClassroom`) had been asserting the *old, wrong* behavior all along — it passed `teacher.getId()` itself as the caller's `userId`, which only "worked" because of the conflation bug being tested against. Corrected to link a real platform `User` to the `Teacher` first, matching what a real caller's token actually carries; a new `teacher_withNoLinkedUserAccount_cannotAccessTheirOwnClassroom` test locks in the fail-closed behavior above.
- ✅ **Testing/CI work — all landed, all blocking, no aspirational thresholds:** 366 tests total across all modules (up from Phase 2's 297), all passing — 79 E2E (14 new: `AdmissionCrudE2ETest`, `StudentBulkImportE2ETest`, `ReportCardE2ETest`, and `TeacherScopedDataAccessE2ETest` — the last one exercising the classroom-narrowing fix through the real HTTP/security stack, two teachers with real linked accounts, one classroom each, asserting each sees only their own classroom's grade/attendance), 68 tenant-isolation/persistence integration tests (9 new, including `AdmissionEnrollmentSagaIntegrationTest` — the literal implementation of this phase's saga "compensable steps" claim: a real duplicate-student-code failure against a real DB proves no orphaned `Student` is left behind and the `Admission` reverts to `APPROVED`, not a mocked assertion that compensation logic runs), 105 `application` unit tests (38 new), 28 `domain` unit tests (8 new: `AdmissionTest`, `AdmissionDecisionTest`, `ReportCardTest`).
  - JaCoCo coverage ratchet **raised again, not just held**: `domain` floor 41% → 54%, `application` floor 62% → 74%.
  - SpotBugs zero-findings baseline held — two more findings investigated and confirmed as the same detector noise pattern Phase 2 already characterized (constructor-injection storing a field reference, flagged inconsistently against structurally identical constructors elsewhere in the same module): `ReportCardService`'s 10-arg constructor (`StorageService`, `ReportCardPdfGenerator`) and `BulkImportResponse`/`BulkImportResult`'s record accessor (`EI_EXPOSE_REP`/`REP2` on an exposed `List` — this one genuinely worth fixing regardless, via a compact constructor `List.copyOf`, since an unprotected mutable list on a response DTO is a real, if minor, encapsulation gap, unlike the constructor-injection findings).
  - `:domain:pitest` scope intentionally unchanged, same reasoning as Phase 2: the new business logic (the saga's orchestration, `ReportCardService`'s date-range aggregation) lives in `application`, not `domain`.
  - OpenAPI snapshot regenerated for the 3 new controllers.
- **Dependencies:** Phase 1 (grades must be real before a report card means anything), Phase 2 (admissions decisions trigger real notifications via `TenantAdminNotifier`) — met.
- **Outcome:** a new tenant can onboard an existing student body in bulk, run a real admissions cycle for new students with genuine compensation on failure, and produce a real, downloadable report card PDF.
- **Validated:** `AdmissionEnrollmentSagaIntegrationTest` (real compensation against a real DB, not mocked), `ReportCardGenerationJobIntegrationTest` (real PDF bytes round-tripped through storage and read back), `TeacherScopedDataAccessE2ETest` (real RBAC narrowing through the full HTTP stack) — each phase's "Validate" language implemented literally, not paraphrased.
- **Deferred, not silently dropped:** `@QuotaCheck` on bulk import (needs a real default plan limit seeded first — platform-data work, not school-saas code); admissions intake stays TENANT_ADMIN-only, no public/self-service application form (§9's "not a real question yet" still holds); report cards aggregate by date-range against `Term`, not a literal `Exam.termId` column — revisit only if a real need for exam-level term tagging (e.g. mid-term transfers) surfaces.

**Phase 4 — Multi-campus — ✅ COMPLETE (2026-08-17).** Organization-level rollup reporting API. (The `Plan.featuresJson`/`limitsJson` entitlement wiring originally scoped here was completed early, 2026-08-16 — see §2/§3; nothing left to do for it in this phase.)

- ✅ `GET /api/v1/organizations/{organizationPublicId}/rollup-report?from=...&to=...` — aggregates active-student enrollment, attendance (present/absent/late/excused, over the `from`/`to` window), and fee balances (a snapshot as of now, same convention as `GET /students/{publicId}/fee-balance`) across every campus (`Tenant`) in a school group (`Organization`), using the `Organization`→`Tenant` hierarchy that already existed platform-side (§3) — the literal "multi-campus is actually usable, not just structurally present" outcome.
- ✅ Domain layer (`domain/rollup/model/`): `AttendanceRollup`, `FeeRollup` (mirrors `FeeBalanceCalculator`'s outstanding/overpaid math, applied at aggregate level), `CampusRollup`, `RollupTotals`, `OrganizationRollupReport` — plain records, zero framework dependencies, each with a `sum`/`of` static factory carrying the actual aggregation math so it's unit-testable in isolation from Spring/JPA.
- ✅ `OrganizationRollupService` (`application/rollup/`) — for each campus in the organization, binds `TenantContext.callAsTenant` to that campus's snapshot in turn (the platform's own sanctioned "act as tenant X for a bounded scope" API, already used by `SchoolTenantProvisioningListener`/`AbstractBaseJob`) and calls the same tenant-scoped repository methods a single-campus request would use — no new cross-tenant query mechanism invented. `FeePaymentRepository.sumPaidAmountByTenantId` added (DB-level `SUM`, not a per-student loop) to keep the fee rollup O(campuses) rather than O(students) — a real N+1 avoided before it was ever written, not fixed after.
- ✅ **A real, non-obvious hazard found and designed around, not discovered in production:** `TenantFilterRequestFilter` binds Hibernate's session-level `tenantFilter` to whichever tenant a request's JWT resolves to, for the whole request — independent of, and with no awareness of, `TenantContext.callAsTenant`'s `ScopedValue` binding. If this endpoint were called with a campus-scoped `TENANT_ADMIN` token, the per-campus loop would silently return zero rows for every campus except the one already bound to the request, since the generated SQL would `AND` two contradictory `tenant_id` predicates together. `OrganizationRollupController` is gated `SUPER_ADMIN`-only (matching platform's own `OrganizationController`) specifically because a system-tenant (`tenant_id=0`) token resolves through `TenantFilterRequestFilter.isTenantRequest = false`, so no Hibernate filter is ever bound for the request — the only token shape this endpoint's cross-tenant reads are correct under. Documented on `OrganizationRollupService`'s class Javadoc, not just here.
- ✅ **Testing/CI work — all landed, all blocking:** 5 domain unit tests (`AttendanceRollupTest`, `FeeRollupTest`, `OrganizationRollupReportTest` — pure aggregation-math assertions, including the "a campus's overpayment must never net against another campus's shortfall" invariant), 6 `application` unit tests (`OrganizationRollupServiceTest`, mocked repositories, including range-validation and organization-not-found cases), 2 `app`-level integration tests (`OrganizationRollupIntegrationTest` — two real campuses under one real `Organization`, real students/attendance/fees, asserting no cross-campus leakage or double counting), 5 E2E tests (`OrganizationRollupE2ETest` — happy path, 401, 403, unknown-organization 404, invalid-range 400). The happy-path E2E test is the actual regression test for the Hibernate-filter hazard above: it goes through the real filter chain with a real SUPER_ADMIN JWT and asserts on real aggregated numbers, which only passes if the hazard is genuinely avoided, not merely reasoned about.
- ✅ OpenAPI snapshot regenerated for the new controller.
- **Dependencies:** Phase 3 for meaningful data to roll up — met.
- **Outcome:** the platform's most distinctive already-built capability (multi-campus) is actually usable, not just structurally present.
- **Validated:** `OrganizationRollupIntegrationTest.generate_aggregatesRealDataAcrossBothCampusesWithoutLeakageOrDoubleCounting` (real DB, real per-campus data, real aggregation) and `OrganizationRollupE2ETest.rollupHappyPath_asSuperAdmin_aggregatesBothCampusesCorrectly` (real HTTP, real JWT, real filter chain) — the literal "no leakage, no double counting, and the cross-tenant read path is genuinely correct under HTTP" claims, not paraphrases of them.
- **Deferred, not silently dropped:** a campus-group business user (e.g. a superintendent) cannot currently get a scoped, non-SUPER_ADMIN rollup view. `OrganizationMembership`'s `ORG_ADMIN` role exists in the platform's domain model but is keyed to `SystemUser` (a platform-operator identity), not the tenant-scoped `User` a real `TENANT_ADMIN` login resolves to — there is no bridge between the two today, and nothing in platform-saas currently authorizes on `OrganizationMembership` at all (`OrganizationController` itself is `SUPER_ADMIN`-only end to end). Building that bridge is platform-side work (a new identity-resolution path, not a school-saas concern) and was out of scope for this phase; this endpoint is real and correct for the only caller shape the platform currently supports for cross-tenant reads.

**Phase 5 — MVP gap closure (architecture-analysis Phase 1) — ✅ COMPLETE (2026-08-18).** Closes the specific, enumerated gaps from `docs/architecture-analysis/10-roadmap.md` §Phase 1 and `09-mvp-definition.md` — a real classroom roster, per-student/classroom fee assignment, the `GRADUATED` transition, `Exam.termId`/`Classroom.academicYearId` as real FKs, update endpoints for the five core aggregates, mandatory idempotency on fee payments, and a configurable (non-console) email provider.

- ✅ **Classroom roster**: new `student_classroom_links` table + `StudentClassroomLink` entity/repository, `ClassroomService.enrollStudent`/`withdrawStudentFromClassroom`/`listRoster`. `AttendanceService.mark()` now rejects a student who isn't on the classroom's roster — closes the "any student markable present in any classroom" gap; previously classroom membership was only ever inferable from attendance history.
- ✅ **Fee assignment**: new `fee_assignments` table (`FeeAssignmentScope.STUDENT`/`CLASSROOM`) + `FeeAssignmentService`. `FeePaymentService.calculateBalanceForStudent` now resolves only the fee structures a student is actually assigned to (directly, or via their current classroom) — not, as before, every tenant fee structure unconditionally. A same-release backfill (`school-025-fee-assignments-backfill`) assigns every pre-existing fee structure to every currently-active student, so no tenant's existing balances silently disappeared when the gating shipped.
- ✅ **Student lifecycle**: `Student.withdraw()`/`graduate()` are now real entity methods (previously `StudentService` reached in and set `enrollmentStatus` + called `softDelete` in the same step — no `graduate()` existed at all, and `withdraw()` had no reachable API endpoint). Withdrawal/graduation no longer soft-deletes — a withdrawn or graduated student stays a real, queryable historical record (past attendance/grades/fees/report cards still reference them), with a new `?status=` filter on `GET /students` to keep the default "active roster" view unchanged.
- ✅ **Real FKs**: `Exam.termId` (nullable — no reliable backfill source existed) and `Classroom.academicYearId` (backfilled from the legacy free-text `academic_year` column by name match, `NOT NULL` applied only once a precondition confirms every row backfilled cleanly; the legacy column stays for one more release per the multi-release rename rule).
- ✅ **Update endpoints**: narrow, per-concern `PATCH` — matching the codebase's existing `AdmissionController`/`SubjectController` convention, not a general PUT-with-full-DTO — added for `Classroom` (teacher reassignment, academic-year move), `Exam` (reschedule, term assignment), `Student` (withdraw, graduate, contact details), `Teacher` (contact details), `FeeStructure` (amount revision, fee assignment CRUD).
- ✅ **Idempotency enforcement**: new platform-level `@RequireIdempotencyKey` annotation + interceptor (`platform-saas`) rejects a POST/PUT/PATCH missing the `Idempotency-Key` header on an annotated endpoint — applied to `FeePaymentController.record`. Complements, rather than replaces, the existing `IdempotencyFilter` (which only replays/dedupes when a caller happens to send the header).
- ✅ **Configurable email**: `ConsoleEmailService` remains the dev default; two real, config-selected providers now exist behind the same `EmailService` interface — `SmtpEmailService` (generic `JavaMailSender`, works against any SMTP-speaking provider) and `SesEmailService` (AWS SES API). Selected via `platform.email.provider` (`smtp`|`ses`), mirroring the existing FCM/APNs conditional-bean pattern; SMTP password / SES static secret key flow through `SecretProvider`, never raw YAML.
- ✅ **Testing** — all landed, all blocking: domain unit tests (`StudentTest` — withdraw/graduate invariants), extended + new `application` unit tests (`ClassroomServiceTest`, `FeeAssignmentServiceTest`, `TeacherServiceTest`, `FeeStructureServiceTest`, `ExamServiceTest`, `StudentServiceTest`, `AttendanceServiceTest`), 2 new `app`-level tenant-isolation integration tests (`ClassroomEnrollmentTenantIsolationIntegrationTest`, `FeeAssignmentTenantIsolationIntegrationTest`), plus every pre-existing test touched by the `Classroom.create`/`Exam.schedule`/`StudentService.withdraw` signature changes updated in place. `platform-saas` gained `IdempotencyRequiredInterceptorTest`, `SmtpEmailServiceTest`, `SesEmailServiceTest`.
- ✅ OpenAPI snapshot regenerated for the new/changed endpoints.
- **Dependencies:** Phase 0 (`docs/architecture-analysis`) — the genericity/notification-provider fixes; met.
- **Outcome:** the 9 MVP acceptance criteria in `docs/architecture-analysis/09-mvp-definition.md` — admit→enroll→attend→examine→grade→report→bill, without needing direct DB access to correct a mistake — are now real, not partial.
- **Validated:** `./gradlew clean build` green in both `platform-saas` and `school-saas` — full test suites, `spotlessCheck`, SpotBugs, JaCoCo coverage-verification, and the OpenAPI snapshot test all blocking and passing.

**Phase 6 — Core School Operations (`docs/architecture-analysis` Phase 2) — ✅ COMPLETE (2026-08-20).** Closes every item in `docs/architecture-analysis/10-roadmap.md` §Phase 2: admissions pipeline completion (public application, entrance-test/merit-list/waitlist), attendance percentage + low-attendance alerts, an LMS core slice (lessons/assignments/submissions), a generic multi-provider payment-gateway architecture with real fee-payment integration and parent/student self-service payment, DB-backed communication templates, and dual-mode guardian self-registration.

- ✅ **Admissions pipeline completion**: `POST /api/v1/admissions/apply` (`permitAll()`) lets a prospective guardian submit without a staff account. `Admission` gained `entranceTestScore`/`entranceTestMaxScore`/`meritRank` (nullable, additive) and a `WAITLISTED` status; `AdmissionService.generateMeritList(appliedGrade, availableSeats)` ranks scored `UNDER_REVIEW` candidates and waitlists the overflow — deliberately *not* tied to a `Classroom.capacity` concept (none exists, and inventing one here would be exactly the over-engineering `10-roadmap.md`'s own risk note warns against); `availableSeats` is an admin-supplied number at merit-list-generation time instead.
- ✅ **Attendance percentage + low-attendance alerts**: `AttendancePercentageCalculator` (a pure domain service, same shape as `FeeBalanceCalculator`) computes `presentDays / totalMarkedDays` — honestly documented as "days attendance was actually marked," since no working-days/school-calendar concept exists to use as a real denominator. New weekly `LowAttendanceAlertJob` compares each roster student's rolling 30-day percentage against a per-tenant threshold (`AttendanceAlertSettingsService`, default 75%, the same `TenantSettingOverrideService`-wrapped pattern `GradingScaleService` already established) and notifies via `NotificationType.LOW_ATTENDANCE_ALERT`.
- ✅ **LMS core slice**: one new `domain/lms` module — `Lesson` (classwork), `Assignment` (homework), `Submission` — deliberately not split into a separate "homework" module, since the two roadmap line items are the same underlying concept. Stops exactly where the roadmap's own risk note said to stop: assignments/submissions only, no live/recorded classes, no quizzes (Phase 3). Reuses the platform's existing generic `POST /api/v1/files/upload` presigned-upload flow for attachments rather than building a parallel storage mechanism. A late submission is automatically marked `LATE`; one submission per student per assignment is enforced at both the app layer and a DB unique constraint.
- ✅ **Payment gateway — new platform capability, not an extension of the existing one**: platform-saas's pre-existing `PaymentProvider` SPI (used for the platform's own Stripe-backed SaaS subscription billing) is architecturally single-tenant-key and shaped around Customer/Subscription — incompatible with per-tenant, multi-provider, one-off charges. Rather than force-fit it, a sibling `domain/paymentgateway`/`application/paymentgateway` capability was added to platform-saas, mirroring the existing configurable-email-provider system exactly: `PaymentGatewayConfig`/`TenantPaymentGatewayConfig` (twin tables, same SUPER_ADMIN-default/TENANT_ADMIN-override precedence as email), a canonical `PaymentGatewayCredentialField` enum (`API_KEY`, `API_SECRET`, `WEBHOOK_SIGNING_SECRET`, ...) so any future gateway's differently-named fields map onto the same generic catalog, `AesEncryptionService`-encrypted credentials (never plaintext, never cached decrypted), a `PaymentGatewayProvider` adapter SPI + `PaymentGatewayProviderRegistry`, and one real reference adapter (`StripePaymentGatewayProvider`, using per-request `RequestOptions` API keys — never the SDK's global static key, since credentials are per-tenant here). Admin/tenant REST mirrors the email pair's endpoint shapes exactly. Adding a second real provider later is a pure addition (one new adapter class), not a redesign.
- ✅ **Fee payment gateway integration + self-service payment**: `FeeOnlinePaymentService` (school-saas) turns a successful gateway charge into a `FeePayment` — school-saas owns the "this student's fee is paid" business meaning, platform owns the gateway mechanics (ADR-004's separation, extended consistently). `FeePayment` gained `paymentSource` (`MANUAL`/`GATEWAY`, default `MANUAL` — every existing row/flow unaffected), `gatewayProviderType`, `gatewayChargeReference` (unique per tenant where present). Both confirmation modes the user explicitly asked for are real and simultaneously available, switchable per tenant via `confirmationMode` (`SYNCHRONOUS`/`WEBHOOK`/`BOTH`) on the same gateway config: a synchronous `POST .../confirm` endpoint, and `POST /api/v1/fee-payments/webhooks/{tenantPublicId}/{providerType}` — a genuinely new pattern (no prior precedent in either repo for a `permitAll` endpoint that resolves tenant context from a path segment), justified because each school tenant has its own gateway account/webhook secret, unlike the platform's own single-account `/api/webhooks/stripe`. Signature verification always happens before tenant context is activated or anything in the payload is trusted; webhook delivery is idempotent (`gatewayChargeReference` uniqueness).
- ✅ **Guardian self-registration — corrected mid-implementation from a `FeatureFlag`-based design to a `TenantSettingOverride`-based one**, per explicit user feedback that entitlement gating (what a tenant's plan allows) and a tenant admin's own operational choice (how self-registration behaves for their school) are different concerns and shouldn't share a mechanism. `GuardianRegistrationSettingsService` (same typed-wrapper shape as `GradingScaleService`) exposes a `TENANT_ADMIN`-only `GET`/`PUT` on `CLAIM_ONLY` (default; only claims an admin-pre-created, unlinked `Guardian` record — grants `PARENT` immediately since the binding is already trusted) vs `OPEN` (also allows minting a brand-new `Guardian` row for an unmatched email, mirroring platform's `AuthController.register()` zero-role convention exactly, since that path has no pre-existing trust to lean on).
- ✅ **Communication templates**: `SchoolTenantProvisioningListener` now seeds one `NotificationTemplate` row per tenant for every school-relevant `NotificationType` (`FEE_DUE`, `EXAM_SCHEDULED`, `LOW_ATTENDANCE_ALERT`, `ASSIGNMENT_POSTED`, `SUBMISSION_GRADED`), idempotent the same way `seedDefaultAcademicYear` already was. `FeePaymentReminderJob`/`ExamScheduleReminderJob`/`LowAttendanceAlertJob`/`AssignmentService`/`SubmissionService` now pass named `templateVariables` instead of building inline strings — the platform's existing `{{placeholder}}` template-resolution engine (already built, previously unused by any real school-saas call site) does the rest; a sensible fallback `title`/`message` is still always populated for a tenant with no customized template. The orphaned, never-wired `app/src/main/resources/templates/notifications/*.html|.txt` files were deleted as dead code.
- ✅ **Platform `NotificationType` additions**: `ASSIGNMENT_POSTED, SUBMISSION_GRADED, LOW_ATTENDANCE_ALERT, APPLICATION_STATUS_CHANGED, MERIT_LIST_PUBLISHED, PAYMENT_RECEIVED, GATEWAY_PAYMENT_FAILED, ACCOUNT_LINKED` — domain-neutral names, a plain enum addition (no schema change, `EnumType.STRING`), passes `PlatformGenericityFitnessTest` unmodified.
- ✅ **Three literal platform `SecurityConfig` permitAll additions**, added deliberately narrow rather than broadly: `/api/v1/admissions/apply` and `/api/v1/guardians/self-register` (both genuinely school-specific — no generic pattern could cover them without opening up unrelated endpoints), plus one new *generic* pattern, `/api/v1/*/webhooks/**`, specifically kept domain-neutral so any future domain consumer's own webhook endpoint benefits automatically rather than needing its own one-off allowlist entry.
- ✅ **A real, repeatable SpotBugs false-positive found and fixed at the source, not suppressed**: `EI_EXPOSE_REP2` fired on several otherwise-unremarkable constructors (`GuardianRegistrationSettingsController`, `FeePaymentWebhookController`, `FeeOnlinePaymentService`) that stored a field of a platform-saas cross-module repository/service type (most consistently, anything typed `TenantRepository`) via a hand-written constructor — while every structurally identical same-repo constructor-injected class passed clean. Switching each flagged class from a manual constructor to Lombok's `@RequiredArgsConstructor` resolved it every time; not fully root-caused (plausibly SpotBugs's mutability analysis of cross-jar auxclasspath types defaulting to "assume mutable" only for hand-written constructors), but consistently reproducible and fixed, not worked around by suppression.
- ✅ **Testing/CI — all landed, all blocking, no aspirational thresholds**: 580 tests total (up from 366 at the end of Phase 5), spanning all three mandatory tiers plus E2E for every new controller (happy path, 401, 403, tenant isolation) on both the school-saas and platform-saas sides. JaCoCo floors held at their Phase-5 level (`domain` 54%, `application` 74%) — not lowered. SpotBugs zero-findings baseline held. `platform-saas`'s own `./gradlew clean build` (89 modules/tasks) and `school-saas`'s (46 tasks) both green independently, and school-saas rebuilt clean against the freshly `publishToMavenLocal`-republished platform artifact after every platform-side change.
- **Dependencies:** Phase 5 (roster, since LMS/attendance-percentage need real classroom membership) — met.
- **Outcome:** the roadmap's own Definition of Done, verified for real: a parent can create a gateway charge, confirm it (both synchronous and webhook paths), and the resulting `FeePayment` is a real receipt; a teacher can post an assignment and see a submission graded end-to-end with a real notification; a prospective parent can submit an admission application with zero staff involvement.
- **Deferred, not silently dropped:** a second real payment-gateway adapter (Razorpay/PayPal/etc.) — the architecture supports one as a pure addition, but none was requested to actually integrate yet; live/recorded classes and quizzes (explicit Phase 3 per the roadmap's own risk note); a school-calendar/working-days concept for a truer attendance-percentage denominator.

**Phase 7 — Advanced Operations (`docs/architecture-analysis` Phase 3) — ✅ COMPLETE (2026-08-24).** Closes every item in `docs/architecture-analysis/10-roadmap.md` §Phase 3: transport, library circulation, inventory/assets, discipline/behavior tracking, events/activities, HR expansion (department/qualification/employment-type), leave management, tenant-admin-configurable multi-board/curriculum modeling with real GPA rollup, role-scoped dashboards (Principal/Finance/HR/Academic), real MFA (TOTP), and real SSO (SAML2 + OIDC login flows). Delivered as six independently-checkpointed sub-phases — see `docs/architecture-analysis/19-phase3-implementation-plan.md` for the full plan/status and `project_phase3_tracking` session history for the per-sub-phase writeups; this section summarizes the school-saas-visible outcome.

- ✅ **Transport/library/inventory/discipline/events**: five new school-saas domain modules — `Vehicle`/`Route`/`RouteStop`/`TransportAssignment`; `Book`/`BookCopy`/`Circulation` with tenant-configurable overdue fines and a weekly reminder job; `Asset`/`AssetAssignment` with a status lifecycle; `DisciplineIncident` with guardian notification; `Event`/`EventRegistration` with capacity enforcement. Each has the same tenant-isolation-test-per-aggregate coverage school-saas's core modules already demonstrate.
- ✅ **HR expansion & leave management**: tenant-admin-defined `Department` (no hardcoded catalog, since departments vary per school) with `Teacher` gaining department/qualification/employment-type; a full leave workflow — tenant-admin-defined `LeaveType`, per-teacher `LeaveBalance`, `LeaveRequest` (submit/approve/reject/cancel, balance deducted only on approval), automatic annual allocation.
- ✅ **Multi-board/curriculum modeling + GPA**, the highest-risk structural change in the phase: replaced the old single-JSON-blob, one-scale-per-tenant grading configuration entirely with first-class `Board`/`Curriculum`/`GradingScale`/`GradingScaleThreshold` entities a tenant admin defines at runtime — a school can run e.g. a CBSE section and an IB section side by side, each on its own grading scale. `Classroom` attaches to a `Curriculum`; grade recording resolves the correct scale through that attachment, falling back to a tenant default and failing fast if neither exists (no silent hardcoded fallback). Real GPA calculation — per term, per academic year, and cumulative — where previously only letter grades existed with no point-value concept.
- ✅ **Role-scoped dashboards**: Principal (school-wide summary), Finance (fee collection/outstanding/fines), HR (staff/leave summary), Academic (grades/GPA/exam-schedule summary) — built by extending platform-saas's existing generic report-execution capability with a new `ReportType.SERVICE_CALL` pluggable data source rather than inventing separate dashboard infrastructure. The same aggregation is also available as a downloadable report through the existing pipeline.
- ✅ **Real MFA (TOTP)**: replaced the schema-only `User.twoFactorEnabled`/`TenantSecuritySettings.enforceMfa` flags (previously unread anywhere) with genuine enrollment, backup codes, a login-time challenge step, and real `enforceMfa` enforcement — an unenrolled user in an enforcing tenant gets a real but restricted session that can only reach enrollment endpoints until enrollment completes. Platform-saas work; school-saas required no changes beyond rebuilding against the republished artifact.
- ✅ **Real SSO (SAML2 + OIDC)**: replaced the "IdP connectivity check only" scaffolding with real SP-initiated login flows for both protocols — OIDC with RS256 id_token signature verification against the IdP's live JWKS; SAML2 with real OpenSAML-based XML signature verification, including an explicit, tested defense against XML Signature Wrapping attacks. Both perform JIT provisioning on first login using the tenant's configured default role. Required adding the Shibboleth Maven repository (OpenSAML is not on Maven Central) to both platform-saas's and school-saas's `build.gradle`. Platform-saas work; school-saas required no changes beyond rebuilding against the republished artifact and regenerating its OpenAPI snapshot (the aggregated API surface picked up the new SSO endpoints).
- **Dependencies:** Phase 6 — met (leave/curriculum/library-fines data the dashboards aggregate all existed already).
- **Outcome:** every capability this file's own §1 "Complete Backend Feature Set" table and §2 prioritization had marked deferred to "Phase 7+" is now real, tested, and shipped — no school-domain P2 module remains speculative or postponed.
- **Validated:** `./gradlew clean build` green in both `platform-saas` and `school-saas` throughout — full test suites (including real end-to-end login-flow tests against stub IdPs for MFA/SSO, not configuration-save tests), `spotlessCheck`, SpotBugs, JaCoCo coverage-verification, OpenAPI snapshot test.

---

## Final Recommendation

1. **Build first:** ~~Phase 0's correctness fixes~~ — done (2026-08-16). ~~Phase 1 (Guardian, real grade/fee logic)~~ — done (2026-08-16). ~~Phase 2 (real scheduling/notifications, Subject/Curriculum, Timetable)~~ — done (2026-08-16). ~~Phase 3 (admissions saga, bulk roster import, report card PDFs)~~ — done (2026-08-16). ~~Phase 4 (multi-campus rollup reporting)~~ — done (2026-08-17). ~~Phase 5 (MVP gap closure)~~ — done (2026-08-18). ~~Phase 6 (Core School Operations — admissions completion, attendance alerts, LMS, payment gateway, communication templates, guardian self-registration)~~ — done (2026-08-20). ~~Phase 7 (Advanced Operations — transport/library/inventory/discipline/events, HR/leave, configurable curriculum+GPA, role-scoped dashboards, real MFA, real SSO)~~ — done (2026-08-24), see Development Roadmap above. No further phases are currently planned.
2. **Platform capabilities to finish before certain school features:** none block Phase 0–3. The one real platform gap identified in this review (`Plan.featuresJson`/`limitsJson` wiring) has since been closed (2026-08-16) — see §2/§3. A second, narrower gap surfaced in Phase 3: `QuotaService` treats an unconfigured metric limit as 0 (blocked), not unlimited — real for any *new* metric a plan has never needed before, not specific to school-saas; tracked as deferred in Phase 3's writeup, not yet blocking anything already shipped.
3. **P0/P1/P2/P3 mapping:** see §2 — P0 is entirely correctness fixes on what exists; P1 is Guardian + real grades/fees + real scheduling; P2 is real scheduling/notifications/Subject/Timetable; P3 is admissions/bulk-import/report-cards, now done — P3 in this section's original numbering predates the phase renumbering above and refers to the multi-campus differentiator (Phase 4).
4. **Platform vs school split:** see §1 and §5 — the split is already mostly right; the only corrections found are two unwired platform capabilities (entitlements, fine-grained permissions), not misplaced code.
5. **Feature flags/plans:** use `FeatureFlagService` and `@QuotaCheck` now, as they exist; don't wait for plan-entitlement wiring to use them.
6. **Architecture to commit to:** modular monolith, cross-module references by ID, events across module boundaries — all already the pattern, just keep it disciplined as new modules are added.
7. **Decisions to make now:** model Guardian as first-class before building more on Student alone; keep FK-by-`Long` (don't introduce `@ManyToOne`) but close the validation gap it currently has.
8. **Deliberately postpone:** AI features, public API surface, schema-per-tenant, partitioning, microservices. (Library, Transport, Inventory, Discipline, and Leave — previously in this list — shipped in Phase 7, 2026-08-24.)
9. **First production-ready milestone:** ~~end of Phase 3~~ — reached (2026-08-16): real grades, real fees, real notifications, bulk onboarding, admissions (with genuine saga compensation), report cards, all correctness-hardened with proper RBAC, 366 tests green.
10. **Avoid at this stage:** everything in §11's "actively avoid" list, and building any P2/P3 module speculatively ahead of the phase sequence above.

**Where I'd push back if you were heading elsewhere:** don't be tempted to build Guardian as a bolt-on later once Student-only workflows are further along — it's cheap now (Phase 1) and expensive after more features assume a two-actor world. Phase 0 closed the structural-correctness-vs-functional-completeness gap (correctness bugs, cross-entity validation, test coverage) on the original 9 modules + `Term` — the modules themselves still don't compute a real letter grade or a real fee balance yet (that's Phase 1's job), so don't read Phase 0's green build as "the domain logic is done." It isn't — it's now provably correct at the layer it operates on, which is a different, narrower claim.

**On "delete and rebuild the whole thing":** don't. The module skeleton is sound by direct inspection — the actual gap is that nothing currently enforces it stays that way (no ArchUnit fitness tests, no coverage gate, no mutation testing running in CI despite being configured). See `TESTING.md` — that's the fix for the "feels like a demo" instinct, not a rewrite.
